<h1>All our flats</h1>

<div class="cards">
  <% @flats.each do |flat| %>
  <div class="card">
    <h2><%= flat.name %></h2>
    <p><%= flat.address %></p>
    <p><%= flat.price_per_night %></p>
    <p><%= flat.number_of_guests %></p>
  </div>
  <% end %>
</div>
